@model IEnumerable<RHWEBAPP2.Models.Experiencia_Laboral>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Empresa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Puesto_Ocupado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_Desde)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_Hasta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Empresa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Puesto_Ocupado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_Desde)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_Hasta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
